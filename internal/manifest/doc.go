// Package manifest owns the library index: fsnotify-driven scanner, per-format
// tag extractors (FLAC / DSF / ALAC / WAV / MP3), SQLite store, and the
// since-filtered manifest serializer consumed by GET /v1/manifest.
package manifest
